// ============================================================
// ADD THESE LINES TO YOUR EXISTING server.js
// ============================================================

// 1) Add this import at the top, with your other imports:
import path from "path";
import { fileURLToPath } from "url";

// 2) Add these two lines right after your imports (needed
//    because you're using ES modules, so __dirname isn't
//    available by default):
const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

// 3) Add this line in your middleware section, right after
//    app.use(express.json()); — this serves uploaded images
//    at http://localhost:5000/uploads/profile-images/xxxx.jpg
app.use("/uploads", express.static(path.join(__dirname, "uploads")));

// ============================================================
// Nothing else in server.js changes. Your existing routes,
// DB connection, and /api/products endpoints stay exactly
// the same.
// ============================================================